# Sourcegraph (OSS) — universal code search across LedgerX
image: { repository: sourcegraph/server, tag: 5.5.0 }
sourcegraph:
  externalURL: https://sourcegraph.ledgerx.internal
  repositoriesToAdd: [github.com/prabhat-roy/LedgerX]
ingress: { enabled: true, hosts: [{ host: sourcegraph.ledgerx.internal }] }
