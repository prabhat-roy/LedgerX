apiVersion: v1
kind: Service
metadata:
  name: {{ include "case-management.fullname" . }}
  labels: {{- include "case-management.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "case-management.selectorLabels" . | nindent 4 }}
