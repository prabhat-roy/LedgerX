apiVersion: v1
kind: Service
metadata:
  name: {{ include "bff-retail.fullname" . }}
  labels: {{- include "bff-retail.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "bff-retail.selectorLabels" . | nindent 4 }}
