apiVersion: v1
kind: Service
metadata:
  name: {{ include "psd2-service.fullname" . }}
  labels: {{- include "psd2-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "psd2-service.selectorLabels" . | nindent 4 }}
