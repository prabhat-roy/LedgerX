apiVersion: v1
kind: Service
metadata:
  name: {{ include "identity-verification.fullname" . }}
  labels: {{- include "identity-verification.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "identity-verification.selectorLabels" . | nindent 4 }}
