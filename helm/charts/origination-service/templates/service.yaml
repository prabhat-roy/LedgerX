apiVersion: v1
kind: Service
metadata:
  name: {{ include "origination-service.fullname" . }}
  labels: {{- include "origination-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "origination-service.selectorLabels" . | nindent 4 }}
