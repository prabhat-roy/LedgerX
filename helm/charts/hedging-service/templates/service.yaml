apiVersion: v1
kind: Service
metadata:
  name: {{ include "hedging-service.fullname" . }}
  labels: {{- include "hedging-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "hedging-service.selectorLabels" . | nindent 4 }}
