apiVersion: v1
kind: Service
metadata:
  name: {{ include "regulatory-reporting.fullname" . }}
  labels: {{- include "regulatory-reporting.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "regulatory-reporting.selectorLabels" . | nindent 4 }}
