apiVersion: v1
kind: Service
metadata:
  name: {{ include "risk-scoring.fullname" . }}
  labels: {{- include "risk-scoring.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "risk-scoring.selectorLabels" . | nindent 4 }}
