apiVersion: v1
kind: Service
metadata:
  name: {{ include "collections-service.fullname" . }}
  labels: {{- include "collections-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "collections-service.selectorLabels" . | nindent 4 }}
