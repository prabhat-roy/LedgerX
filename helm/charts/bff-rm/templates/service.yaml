apiVersion: v1
kind: Service
metadata:
  name: {{ include "bff-rm.fullname" . }}
  labels: {{- include "bff-rm.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "bff-rm.selectorLabels" . | nindent 4 }}
