apiVersion: v1
kind: Service
metadata:
  name: {{ include "ledger-service.fullname" . }}
  labels: {{- include "ledger-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "ledger-service.selectorLabels" . | nindent 4 }}
