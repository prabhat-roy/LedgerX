apiVersion: v1
kind: Service
metadata:
  name: {{ include "payment-router.fullname" . }}
  labels: {{- include "payment-router.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "payment-router.selectorLabels" . | nindent 4 }}
