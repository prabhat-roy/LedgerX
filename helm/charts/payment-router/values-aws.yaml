cloud: { provider: aws, storageClass: gp3, ingressClass: alb, regions: [us-east-1, eu-west-1, ap-south-1], multiRegion: active-active }
serviceAccount:
  annotations:
    eks.amazonaws.com/role-arn: arn:aws:iam::{{ .Values.aws.accountId }}:role/ledgerx-payment-router
envFrom:
  - secretRef: { name: payment-router-aws-secrets }
