apiVersion: v1
kind: Service
metadata:
  name: {{ include "document-ocr.fullname" . }}
  labels: {{- include "document-ocr.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "document-ocr.selectorLabels" . | nindent 4 }}
