apiVersion: v1
kind: Service
metadata:
  name: {{ include "device-trust.fullname" . }}
  labels: {{- include "device-trust.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "device-trust.selectorLabels" . | nindent 4 }}
