apiVersion: v1
kind: Service
metadata:
  name: {{ include "hold-service.fullname" . }}
  labels: {{- include "hold-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "hold-service.selectorLabels" . | nindent 4 }}
