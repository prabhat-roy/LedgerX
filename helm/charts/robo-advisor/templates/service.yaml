apiVersion: v1
kind: Service
metadata:
  name: {{ include "robo-advisor.fullname" . }}
  labels: {{- include "robo-advisor.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "robo-advisor.selectorLabels" . | nindent 4 }}
