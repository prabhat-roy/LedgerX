apiVersion: v1
kind: Service
metadata:
  name: {{ include "fx-service.fullname" . }}
  labels: {{- include "fx-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "fx-service.selectorLabels" . | nindent 4 }}
