apiVersion: v1
kind: Service
metadata:
  name: {{ include "reconciliation-service.fullname" . }}
  labels: {{- include "reconciliation-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "reconciliation-service.selectorLabels" . | nindent 4 }}
