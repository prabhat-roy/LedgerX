apiVersion: v1
kind: Service
metadata:
  name: {{ include "tpp-registry.fullname" . }}
  labels: {{- include "tpp-registry.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "tpp-registry.selectorLabels" . | nindent 4 }}
