cloud:
  provider: gcp
  storageClass: pd-balanced
  ingressClass: gce

serviceAccount:
  annotations:
    iam.gke.io/gcp-service-account: ledgerx-tpp-registry@{{ .Values.gcp.project }}.iam.gserviceaccount.com
