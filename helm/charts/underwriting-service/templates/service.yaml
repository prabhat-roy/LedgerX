apiVersion: v1
kind: Service
metadata:
  name: {{ include "underwriting-service.fullname" . }}
  labels: {{- include "underwriting-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "underwriting-service.selectorLabels" . | nindent 4 }}
