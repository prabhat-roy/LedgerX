apiVersion: v1
kind: Service
metadata:
  name: {{ include "api-gateway-ob.fullname" . }}
  labels: {{- include "api-gateway-ob.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "api-gateway-ob.selectorLabels" . | nindent 4 }}
