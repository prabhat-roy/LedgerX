apiVersion: v1
kind: Service
metadata:
  name: {{ include "financial-rules-engine.fullname" . }}
  labels: {{- include "financial-rules-engine.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "financial-rules-engine.selectorLabels" . | nindent 4 }}
