apiVersion: v1
kind: Service
metadata:
  name: {{ include "rate-limiter.fullname" . }}
  labels: {{- include "rate-limiter.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "rate-limiter.selectorLabels" . | nindent 4 }}
