apiVersion: v1
kind: Service
metadata:
  name: {{ include "relationship-service.fullname" . }}
  labels: {{- include "relationship-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "relationship-service.selectorLabels" . | nindent 4 }}
