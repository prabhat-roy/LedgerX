apiVersion: v1
kind: Service
metadata:
  name: {{ include "config-proxy.fullname" . }}
  labels: {{- include "config-proxy.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "config-proxy.selectorLabels" . | nindent 4 }}
