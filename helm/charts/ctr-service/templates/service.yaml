apiVersion: v1
kind: Service
metadata:
  name: {{ include "ctr-service.fullname" . }}
  labels: {{- include "ctr-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "ctr-service.selectorLabels" . | nindent 4 }}
