apiVersion: v1
kind: Service
metadata:
  name: {{ include "disbursement-service.fullname" . }}
  labels: {{- include "disbursement-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "disbursement-service.selectorLabels" . | nindent 4 }}
