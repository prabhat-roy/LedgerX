apiVersion: v1
kind: Service
metadata:
  name: {{ include "cash-pooling.fullname" . }}
  labels: {{- include "cash-pooling.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "cash-pooling.selectorLabels" . | nindent 4 }}
