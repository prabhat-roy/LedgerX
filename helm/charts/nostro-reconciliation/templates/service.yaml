apiVersion: v1
kind: Service
metadata:
  name: {{ include "nostro-reconciliation.fullname" . }}
  labels: {{- include "nostro-reconciliation.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "nostro-reconciliation.selectorLabels" . | nindent 4 }}
