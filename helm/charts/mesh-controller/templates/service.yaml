apiVersion: v1
kind: Service
metadata:
  name: {{ include "mesh-controller.fullname" . }}
  labels: {{- include "mesh-controller.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "mesh-controller.selectorLabels" . | nindent 4 }}
