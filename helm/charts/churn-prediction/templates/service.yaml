apiVersion: v1
kind: Service
metadata:
  name: {{ include "churn-prediction.fullname" . }}
  labels: {{- include "churn-prediction.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "churn-prediction.selectorLabels" . | nindent 4 }}
