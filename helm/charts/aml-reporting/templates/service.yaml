apiVersion: v1
kind: Service
metadata:
  name: {{ include "aml-reporting.fullname" . }}
  labels: {{- include "aml-reporting.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "aml-reporting.selectorLabels" . | nindent 4 }}
