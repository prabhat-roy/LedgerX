apiVersion: v1
kind: Service
metadata:
  name: {{ include "statement-service.fullname" . }}
  labels: {{- include "statement-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "statement-service.selectorLabels" . | nindent 4 }}
