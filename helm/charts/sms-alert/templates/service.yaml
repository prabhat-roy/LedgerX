apiVersion: v1
kind: Service
metadata:
  name: {{ include "sms-alert.fullname" . }}
  labels: {{- include "sms-alert.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "sms-alert.selectorLabels" . | nindent 4 }}
