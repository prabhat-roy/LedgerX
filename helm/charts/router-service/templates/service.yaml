apiVersion: v1
kind: Service
metadata:
  name: {{ include "router-service.fullname" . }}
  labels: {{- include "router-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "router-service.selectorLabels" . | nindent 4 }}
