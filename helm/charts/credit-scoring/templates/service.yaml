apiVersion: v1
kind: Service
metadata:
  name: {{ include "credit-scoring.fullname" . }}
  labels: {{- include "credit-scoring.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "credit-scoring.selectorLabels" . | nindent 4 }}
