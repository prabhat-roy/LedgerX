cloud: { provider: azure, regions: [eastus, westeurope, centralindia], multiRegion: active-active }
serviceAccount: { annotations: { azure.workload.identity/client-id: "{{ .Values.azure.clientId }}" } }
podLabels: { azure.workload.identity/use: "true" }
envFrom: [ { secretRef: { name: saga-orchestrator-azure-secrets } } ]
