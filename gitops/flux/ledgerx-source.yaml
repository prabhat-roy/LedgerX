apiVersion: source.toolkit.fluxcd.io/v1
kind: GitRepository
metadata: { name: ledgerx, namespace: flux-system }
spec:
  interval: 1m
  url: https://github.com/prabhat-roy/LedgerX
  ref: { branch: main }
  ignore: |
    /*
    !/helm
    !/manifests
