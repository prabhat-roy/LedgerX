# Apache Polaris — Iceberg REST catalog with RBAC + multi-engine support
image: { repository: apache/polaris, tag: latest }
polaris:
  realm: ledgerx
  storage:
    s3: { endpoint: http://minio.ledgerx.svc:9000, region: us-east-1, bucket: ledgerx-iceberg-warehouse }
ingress: { enabled: true, hosts: [{ host: polaris.ledgerx.internal }] }
