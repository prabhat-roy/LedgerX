apiVersion: chaos-mesh.org/v1alpha1
kind: TimeChaos
metadata: { name: ledgerx-clock-skew, namespace: chaos-mesh }
spec:
  mode: one
  selector: { namespaces: [ledgerx] }
  timeOffset: "+500ms"
  duration: 3m
