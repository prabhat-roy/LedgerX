apiVersion: chaos-mesh.org/v1alpha1
kind: StressChaos
metadata: { name: ledgerx-cpu-stress, namespace: chaos-mesh }
spec:
  mode: one
  selector: { namespaces: [ledgerx] }
  stressors: { cpu: { workers: 4, load: 80 } }
  duration: 5m
