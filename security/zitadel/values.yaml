# ZITADEL — developer-platform auth (API keys, OAuth clients, dev portal SSO)
image: { repository: ghcr.io/zitadel/zitadel, tag: latest }
zitadel:
  externalDomain: zitadel.ledgerx.internal
  externalSecure: true
  database: { postgres: { host: postgres.ledgerx.svc, database: zitadel } }
ingress: { enabled: true, hosts: [{ host: zitadel.ledgerx.internal }] }
