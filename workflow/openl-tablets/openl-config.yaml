# OpenL Tablets — Excel-driven business rules (used for credit-scoring tables)
image: { repository: openltablets/webstudio, tag: 5.27.7 }
ingress:
  enabled: true
  hosts: [{ host: openl.ledgerx.internal }]
