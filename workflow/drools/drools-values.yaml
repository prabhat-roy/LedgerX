# Drools / KIE Server — business rules engine for credit + lending decisions
image: { repository: kiegroup/kie-server, tag: 7.74.1.Final }
postgresql: { enabled: true }
ingress:
  enabled: true
  hosts: [{ host: drools.ledgerx.internal }]
