apiVersion: v1
kind: Service
metadata:
  name: api-gateway-ob
  namespace: ledgerx
  labels: { app.kubernetes.io/name: api-gateway-ob, ledgerx.io/domain: open-banking }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50241, targetPort: 50241 }]
  selector: { app.kubernetes.io/name: api-gateway-ob }
