apiVersion: apps/v1
kind: Deployment
metadata:
  name: psd2-service
  namespace: ledgerx
  labels:
    app.kubernetes.io/name: psd2-service
    app.kubernetes.io/part-of: ledgerx
    ledgerx.io/domain: open-banking
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: psd2-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: psd2-service, ledgerx.io/domain: open-banking }
    spec:
      serviceAccountName: psd2-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: psd2-service
          image: harbor.ledgerx.internal/ledgerx/psd2-service:latest
          ports: [{ containerPort: 50243, name: http }]
          envFrom: [{ configMapRef: { name: psd2-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50243 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50243 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
