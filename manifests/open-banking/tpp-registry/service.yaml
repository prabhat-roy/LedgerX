apiVersion: v1
kind: Service
metadata:
  name: tpp-registry
  namespace: ledgerx
  labels: { app.kubernetes.io/name: tpp-registry, ledgerx.io/domain: open-banking }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50242, targetPort: 50242 }]
  selector: { app.kubernetes.io/name: tpp-registry }
