apiVersion: apps/v1
kind: Deployment
metadata:
  name: relationship-service
  namespace: ledgerx
  labels:
    app.kubernetes.io/name: relationship-service
    app.kubernetes.io/part-of: ledgerx
    ledgerx.io/domain: customer-360
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: relationship-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: relationship-service, ledgerx.io/domain: customer-360 }
    spec:
      serviceAccountName: relationship-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: relationship-service
          image: harbor.ledgerx.internal/ledgerx/relationship-service:latest
          ports: [{ containerPort: 50281, name: http }]
          envFrom: [{ configMapRef: { name: relationship-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50281 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50281 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
