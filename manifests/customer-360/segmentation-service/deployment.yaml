apiVersion: apps/v1
kind: Deployment
metadata:
  name: segmentation-service
  namespace: ledgerx
  labels:
    app.kubernetes.io/name: segmentation-service
    app.kubernetes.io/part-of: ledgerx
    ledgerx.io/domain: customer-360
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: segmentation-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: segmentation-service, ledgerx.io/domain: customer-360 }
    spec:
      serviceAccountName: segmentation-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: segmentation-service
          image: harbor.ledgerx.internal/ledgerx/segmentation-service:latest
          ports: [{ containerPort: 50282, name: http }]
          envFrom: [{ configMapRef: { name: segmentation-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50282 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50282 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
