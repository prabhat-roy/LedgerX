apiVersion: v1
kind: Service
metadata:
  name: segmentation-service
  namespace: ledgerx
  labels: { app.kubernetes.io/name: segmentation-service, ledgerx.io/domain: customer-360 }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50282, targetPort: 50282 }]
  selector: { app.kubernetes.io/name: segmentation-service }
