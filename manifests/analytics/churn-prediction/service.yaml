apiVersion: v1
kind: Service
metadata:
  name: churn-prediction
  namespace: ledgerx
  labels: { app.kubernetes.io/name: churn-prediction, ledgerx.io/domain: analytics }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50301, targetPort: 50301 }]
  selector: { app.kubernetes.io/name: churn-prediction }
