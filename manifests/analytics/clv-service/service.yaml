apiVersion: v1
kind: Service
metadata:
  name: clv-service
  namespace: ledgerx
  labels: { app.kubernetes.io/name: clv-service, ledgerx.io/domain: analytics }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50302, targetPort: 50302 }]
  selector: { app.kubernetes.io/name: clv-service }
