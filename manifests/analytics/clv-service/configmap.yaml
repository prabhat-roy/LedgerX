apiVersion: v1
kind: ConfigMap
metadata:
  name: clv-service
  namespace: ledgerx
  labels: { app.kubernetes.io/name: clv-service, ledgerx.io/domain: analytics }
data:
  LOG_LEVEL: info
  PORT: "50302"
