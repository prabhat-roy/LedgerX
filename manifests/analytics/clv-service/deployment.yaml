apiVersion: apps/v1
kind: Deployment
metadata:
  name: clv-service
  namespace: ledgerx
  labels:
    app.kubernetes.io/name: clv-service
    app.kubernetes.io/part-of: ledgerx
    ledgerx.io/domain: analytics
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: clv-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: clv-service, ledgerx.io/domain: analytics }
    spec:
      serviceAccountName: clv-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: clv-service
          image: harbor.ledgerx.internal/ledgerx/clv-service:latest
          ports: [{ containerPort: 50302, name: http }]
          envFrom: [{ configMapRef: { name: clv-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50302 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50302 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
