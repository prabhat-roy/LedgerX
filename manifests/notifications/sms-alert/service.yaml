apiVersion: v1
kind: Service
metadata:
  name: sms-alert
  namespace: ledgerx
  labels: { app.kubernetes.io/name: sms-alert, ledgerx.io/domain: notifications }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50261, targetPort: 50261 }]
  selector: { app.kubernetes.io/name: sms-alert }
