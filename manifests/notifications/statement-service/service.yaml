apiVersion: v1
kind: Service
metadata:
  name: statement-service
  namespace: ledgerx
  labels: { app.kubernetes.io/name: statement-service, ledgerx.io/domain: notifications }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50263, targetPort: 50263 }]
  selector: { app.kubernetes.io/name: statement-service }
