apiVersion: apps/v1
kind: Deployment
metadata:
  name: statement-service
  namespace: ledgerx
  labels:
    app.kubernetes.io/name: statement-service
    app.kubernetes.io/part-of: ledgerx
    ledgerx.io/domain: notifications
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: statement-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: statement-service, ledgerx.io/domain: notifications }
    spec:
      serviceAccountName: statement-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: statement-service
          image: harbor.ledgerx.internal/ledgerx/statement-service:latest
          ports: [{ containerPort: 50263, name: http }]
          envFrom: [{ configMapRef: { name: statement-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50263 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50263 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
