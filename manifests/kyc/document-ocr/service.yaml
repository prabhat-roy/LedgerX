apiVersion: v1
kind: Service
metadata:
  name: document-ocr
  namespace: ledgerx
  labels: { app.kubernetes.io/name: document-ocr, ledgerx.io/domain: kyc }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50122, targetPort: 50122 }]
  selector: { app.kubernetes.io/name: document-ocr }
