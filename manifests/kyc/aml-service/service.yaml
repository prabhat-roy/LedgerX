apiVersion: v1
kind: Service
metadata:
  name: aml-service
  namespace: ledgerx
  labels: { app.kubernetes.io/name: aml-service, ledgerx.io/domain: kyc }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50121, targetPort: 50121 }]
  selector: { app.kubernetes.io/name: aml-service }
