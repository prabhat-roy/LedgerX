apiVersion: v1
kind: Service
metadata:
  name: identity-verification
  namespace: ledgerx
  labels: { app.kubernetes.io/name: identity-verification, ledgerx.io/domain: kyc }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50120, targetPort: 50120 }]
  selector: { app.kubernetes.io/name: identity-verification }
