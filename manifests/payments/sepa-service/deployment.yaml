apiVersion: apps/v1
kind: Deployment
metadata:
  name: sepa-service
  namespace: ledgerx
  labels:
    app.kubernetes.io/name: sepa-service
    app.kubernetes.io/part-of: ledgerx
    ledgerx.io/domain: payments
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: sepa-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: sepa-service, ledgerx.io/domain: payments }
    spec:
      serviceAccountName: sepa-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: sepa-service
          image: harbor.ledgerx.internal/ledgerx/sepa-service:latest
          ports: [{ containerPort: 50063, name: http }]
          envFrom: [{ configMapRef: { name: sepa-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50063 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50063 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
