apiVersion: v1
kind: Service
metadata:
  name: sepa-service
  namespace: ledgerx
  labels: { app.kubernetes.io/name: sepa-service, ledgerx.io/domain: payments }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50063, targetPort: 50063 }]
  selector: { app.kubernetes.io/name: sepa-service }
