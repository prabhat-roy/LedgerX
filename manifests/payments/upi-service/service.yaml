apiVersion: v1
kind: Service
metadata:
  name: upi-service
  namespace: ledgerx
  labels: { app.kubernetes.io/name: upi-service, ledgerx.io/domain: payments }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50064, targetPort: 50064 }]
  selector: { app.kubernetes.io/name: upi-service }
