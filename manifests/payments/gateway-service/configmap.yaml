apiVersion: v1
kind: ConfigMap
metadata:
  name: gateway-service
  namespace: ledgerx
  labels: { app.kubernetes.io/name: gateway-service, ledgerx.io/domain: payments }
data:
  LOG_LEVEL: info
  PORT: "50060"
