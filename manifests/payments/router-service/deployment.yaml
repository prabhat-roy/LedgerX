apiVersion: apps/v1
kind: Deployment
metadata:
  name: router-service
  namespace: ledgerx
  labels:
    app.kubernetes.io/name: router-service
    app.kubernetes.io/part-of: ledgerx
    ledgerx.io/domain: payments
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: router-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: router-service, ledgerx.io/domain: payments }
    spec:
      serviceAccountName: router-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: router-service
          image: harbor.ledgerx.internal/ledgerx/router-service:latest
          ports: [{ containerPort: 50061, name: http }]
          envFrom: [{ configMapRef: { name: router-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50061 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50061 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
