apiVersion: apps/v1
kind: Deployment
metadata:
  name: swift-service
  namespace: ledgerx
  labels:
    app.kubernetes.io/name: swift-service
    app.kubernetes.io/part-of: ledgerx
    ledgerx.io/domain: payments
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: swift-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: swift-service, ledgerx.io/domain: payments }
    spec:
      serviceAccountName: swift-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: swift-service
          image: harbor.ledgerx.internal/ledgerx/swift-service:latest
          ports: [{ containerPort: 50062, name: http }]
          envFrom: [{ configMapRef: { name: swift-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50062 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50062 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
