apiVersion: v1
kind: ConfigMap
metadata:
  name: swift-service
  namespace: ledgerx
  labels: { app.kubernetes.io/name: swift-service, ledgerx.io/domain: payments }
data:
  LOG_LEVEL: info
  PORT: "50062"
