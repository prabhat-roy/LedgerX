apiVersion: v1
kind: Service
metadata:
  name: swift-service
  namespace: ledgerx
  labels: { app.kubernetes.io/name: swift-service, ledgerx.io/domain: payments }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50062, targetPort: 50062 }]
  selector: { app.kubernetes.io/name: swift-service }
