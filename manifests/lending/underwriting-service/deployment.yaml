apiVersion: apps/v1
kind: Deployment
metadata:
  name: underwriting-service
  namespace: ledgerx
  labels:
    app.kubernetes.io/name: underwriting-service
    app.kubernetes.io/part-of: ledgerx
    ledgerx.io/domain: lending
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: underwriting-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: underwriting-service, ledgerx.io/domain: lending }
    spec:
      serviceAccountName: underwriting-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: underwriting-service
          image: harbor.ledgerx.internal/ledgerx/underwriting-service:latest
          ports: [{ containerPort: 50101, name: http }]
          envFrom: [{ configMapRef: { name: underwriting-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50101 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50101 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
