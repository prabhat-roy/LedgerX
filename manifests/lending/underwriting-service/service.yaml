apiVersion: v1
kind: Service
metadata:
  name: underwriting-service
  namespace: ledgerx
  labels: { app.kubernetes.io/name: underwriting-service, ledgerx.io/domain: lending }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50101, targetPort: 50101 }]
  selector: { app.kubernetes.io/name: underwriting-service }
