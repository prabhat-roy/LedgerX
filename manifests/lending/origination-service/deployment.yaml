apiVersion: apps/v1
kind: Deployment
metadata:
  name: origination-service
  namespace: ledgerx
  labels:
    app.kubernetes.io/name: origination-service
    app.kubernetes.io/part-of: ledgerx
    ledgerx.io/domain: lending
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: origination-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: origination-service, ledgerx.io/domain: lending }
    spec:
      serviceAccountName: origination-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: origination-service
          image: harbor.ledgerx.internal/ledgerx/origination-service:latest
          ports: [{ containerPort: 50100, name: http }]
          envFrom: [{ configMapRef: { name: origination-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50100 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50100 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
