apiVersion: v1
kind: Service
metadata:
  name: origination-service
  namespace: ledgerx
  labels: { app.kubernetes.io/name: origination-service, ledgerx.io/domain: lending }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50100, targetPort: 50100 }]
  selector: { app.kubernetes.io/name: origination-service }
