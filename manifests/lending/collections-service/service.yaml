apiVersion: v1
kind: Service
metadata:
  name: collections-service
  namespace: ledgerx
  labels: { app.kubernetes.io/name: collections-service, ledgerx.io/domain: lending }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50104, targetPort: 50104 }]
  selector: { app.kubernetes.io/name: collections-service }
