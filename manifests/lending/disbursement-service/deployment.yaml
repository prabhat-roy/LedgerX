apiVersion: apps/v1
kind: Deployment
metadata:
  name: disbursement-service
  namespace: ledgerx
  labels:
    app.kubernetes.io/name: disbursement-service
    app.kubernetes.io/part-of: ledgerx
    ledgerx.io/domain: lending
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: disbursement-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: disbursement-service, ledgerx.io/domain: lending }
    spec:
      serviceAccountName: disbursement-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: disbursement-service
          image: harbor.ledgerx.internal/ledgerx/disbursement-service:latest
          ports: [{ containerPort: 50103, name: http }]
          envFrom: [{ configMapRef: { name: disbursement-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50103 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50103 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
