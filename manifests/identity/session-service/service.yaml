apiVersion: v1
kind: Service
metadata:
  name: session-service
  namespace: ledgerx
  labels: { app.kubernetes.io/name: session-service, ledgerx.io/domain: identity }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50022, targetPort: 50022 }]
  selector: { app.kubernetes.io/name: session-service }
