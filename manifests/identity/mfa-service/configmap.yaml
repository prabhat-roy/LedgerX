apiVersion: v1
kind: ConfigMap
metadata:
  name: mfa-service
  namespace: ledgerx
  labels: { app.kubernetes.io/name: mfa-service, ledgerx.io/domain: identity }
data:
  LOG_LEVEL: info
  PORT: "50021"
