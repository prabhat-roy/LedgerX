apiVersion: v1
kind: Service
metadata:
  name: device-trust
  namespace: ledgerx
  labels: { app.kubernetes.io/name: device-trust, ledgerx.io/domain: identity }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50023, targetPort: 50023 }]
  selector: { app.kubernetes.io/name: device-trust }
