apiVersion: v1
kind: ConfigMap
metadata:
  name: device-trust
  namespace: ledgerx
  labels: { app.kubernetes.io/name: device-trust, ledgerx.io/domain: identity }
data:
  LOG_LEVEL: info
  PORT: "50023"
