apiVersion: v1
kind: Service
metadata:
  name: virtual-card
  namespace: ledgerx
  labels: { app.kubernetes.io/name: virtual-card, ledgerx.io/domain: cards }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50083, targetPort: 50083 }]
  selector: { app.kubernetes.io/name: virtual-card }
