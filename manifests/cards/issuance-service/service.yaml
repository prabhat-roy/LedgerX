apiVersion: v1
kind: Service
metadata:
  name: issuance-service
  namespace: ledgerx
  labels: { app.kubernetes.io/name: issuance-service, ledgerx.io/domain: cards }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50080, targetPort: 50080 }]
  selector: { app.kubernetes.io/name: issuance-service }
