apiVersion: v1
kind: ConfigMap
metadata:
  name: issuance-service
  namespace: ledgerx
  labels: { app.kubernetes.io/name: issuance-service, ledgerx.io/domain: cards }
data:
  LOG_LEVEL: info
  PORT: "50080"
