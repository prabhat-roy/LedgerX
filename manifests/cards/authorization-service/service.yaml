apiVersion: v1
kind: Service
metadata:
  name: authorization-service
  namespace: ledgerx
  labels: { app.kubernetes.io/name: authorization-service, ledgerx.io/domain: cards }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50081, targetPort: 50081 }]
  selector: { app.kubernetes.io/name: authorization-service }
