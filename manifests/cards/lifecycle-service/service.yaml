apiVersion: v1
kind: Service
metadata:
  name: lifecycle-service
  namespace: ledgerx
  labels: { app.kubernetes.io/name: lifecycle-service, ledgerx.io/domain: cards }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50082, targetPort: 50082 }]
  selector: { app.kubernetes.io/name: lifecycle-service }
