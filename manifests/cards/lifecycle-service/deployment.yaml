apiVersion: apps/v1
kind: Deployment
metadata:
  name: lifecycle-service
  namespace: ledgerx
  labels:
    app.kubernetes.io/name: lifecycle-service
    app.kubernetes.io/part-of: ledgerx
    ledgerx.io/domain: cards
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: lifecycle-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: lifecycle-service, ledgerx.io/domain: cards }
    spec:
      serviceAccountName: lifecycle-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: lifecycle-service
          image: harbor.ledgerx.internal/ledgerx/lifecycle-service:latest
          ports: [{ containerPort: 50082, name: http }]
          envFrom: [{ configMapRef: { name: lifecycle-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50082 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50082 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
