apiVersion: v1
kind: Service
metadata: { name: ledger-service, namespace: ledgerx }
spec:
  type: ClusterIP
  ports: [{ port: 60021, targetPort: http, name: http }]
  selector: { app: ledger-service }
