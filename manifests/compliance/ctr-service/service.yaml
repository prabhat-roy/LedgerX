apiVersion: v1
kind: Service
metadata:
  name: ctr-service
  namespace: ledgerx
  labels: { app.kubernetes.io/name: ctr-service, ledgerx.io/domain: compliance }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50221, targetPort: 50221 }]
  selector: { app.kubernetes.io/name: ctr-service }
