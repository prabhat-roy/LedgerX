apiVersion: apps/v1
kind: Deployment
metadata:
  name: ctr-service
  namespace: ledgerx
  labels:
    app.kubernetes.io/name: ctr-service
    app.kubernetes.io/part-of: ledgerx
    ledgerx.io/domain: compliance
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: ctr-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: ctr-service, ledgerx.io/domain: compliance }
    spec:
      serviceAccountName: ctr-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: ctr-service
          image: harbor.ledgerx.internal/ledgerx/ctr-service:latest
          ports: [{ containerPort: 50221, name: http }]
          envFrom: [{ configMapRef: { name: ctr-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50221 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50221 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
