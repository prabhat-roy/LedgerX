apiVersion: apps/v1
kind: Deployment
metadata:
  name: sar-service
  namespace: ledgerx
  labels:
    app.kubernetes.io/name: sar-service
    app.kubernetes.io/part-of: ledgerx
    ledgerx.io/domain: compliance
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: sar-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: sar-service, ledgerx.io/domain: compliance }
    spec:
      serviceAccountName: sar-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: sar-service
          image: harbor.ledgerx.internal/ledgerx/sar-service:latest
          ports: [{ containerPort: 50222, name: http }]
          envFrom: [{ configMapRef: { name: sar-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50222 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50222 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
