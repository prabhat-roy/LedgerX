apiVersion: v1
kind: Service
metadata:
  name: sar-service
  namespace: ledgerx
  labels: { app.kubernetes.io/name: sar-service, ledgerx.io/domain: compliance }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50222, targetPort: 50222 }]
  selector: { app.kubernetes.io/name: sar-service }
