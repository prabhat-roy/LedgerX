apiVersion: apps/v1
kind: Deployment
metadata:
  name: aml-reporting
  namespace: ledgerx
  labels:
    app.kubernetes.io/name: aml-reporting
    app.kubernetes.io/part-of: ledgerx
    ledgerx.io/domain: compliance
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: aml-reporting } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: aml-reporting, ledgerx.io/domain: compliance }
    spec:
      serviceAccountName: aml-reporting
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: aml-reporting
          image: harbor.ledgerx.internal/ledgerx/aml-reporting:latest
          ports: [{ containerPort: 50220, name: http }]
          envFrom: [{ configMapRef: { name: aml-reporting } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50220 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50220 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
