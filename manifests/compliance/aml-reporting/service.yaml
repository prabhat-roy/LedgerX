apiVersion: v1
kind: Service
metadata:
  name: aml-reporting
  namespace: ledgerx
  labels: { app.kubernetes.io/name: aml-reporting, ledgerx.io/domain: compliance }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50220, targetPort: 50220 }]
  selector: { app.kubernetes.io/name: aml-reporting }
