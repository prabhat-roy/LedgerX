apiVersion: v1
kind: Service
metadata:
  name: regulatory-reporting
  namespace: ledgerx
  labels: { app.kubernetes.io/name: regulatory-reporting, ledgerx.io/domain: compliance }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50223, targetPort: 50223 }]
  selector: { app.kubernetes.io/name: regulatory-reporting }
