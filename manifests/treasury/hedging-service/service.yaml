apiVersion: v1
kind: Service
metadata:
  name: hedging-service
  namespace: ledgerx
  labels: { app.kubernetes.io/name: hedging-service, ledgerx.io/domain: treasury }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50203, targetPort: 50203 }]
  selector: { app.kubernetes.io/name: hedging-service }
