apiVersion: v1
kind: Service
metadata:
  name: cash-pooling
  namespace: ledgerx
  labels: { app.kubernetes.io/name: cash-pooling, ledgerx.io/domain: treasury }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50202, targetPort: 50202 }]
  selector: { app.kubernetes.io/name: cash-pooling }
