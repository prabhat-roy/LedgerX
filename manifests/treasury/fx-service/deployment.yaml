apiVersion: apps/v1
kind: Deployment
metadata:
  name: fx-service
  namespace: ledgerx
  labels:
    app.kubernetes.io/name: fx-service
    app.kubernetes.io/part-of: ledgerx
    ledgerx.io/domain: treasury
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: fx-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: fx-service, ledgerx.io/domain: treasury }
    spec:
      serviceAccountName: fx-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: fx-service
          image: harbor.ledgerx.internal/ledgerx/fx-service:latest
          ports: [{ containerPort: 50200, name: http }]
          envFrom: [{ configMapRef: { name: fx-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50200 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50200 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
