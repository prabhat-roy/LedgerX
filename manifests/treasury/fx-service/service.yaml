apiVersion: v1
kind: Service
metadata:
  name: fx-service
  namespace: ledgerx
  labels: { app.kubernetes.io/name: fx-service, ledgerx.io/domain: treasury }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50200, targetPort: 50200 }]
  selector: { app.kubernetes.io/name: fx-service }
