apiVersion: v1
kind: Service
metadata:
  name: liquidity-service
  namespace: ledgerx
  labels: { app.kubernetes.io/name: liquidity-service, ledgerx.io/domain: treasury }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50201, targetPort: 50201 }]
  selector: { app.kubernetes.io/name: liquidity-service }
