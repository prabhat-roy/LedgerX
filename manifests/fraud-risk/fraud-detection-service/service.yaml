apiVersion: v1
kind: Service
metadata: { name: fraud-detection-service, namespace: ledgerx }
spec: { type: ClusterIP, ports: [{ port: 60120, targetPort: http, name: http }], selector: { app: fraud-detection-service } }
