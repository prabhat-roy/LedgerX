apiVersion: v1
kind: Service
metadata:
  name: transaction-monitoring
  namespace: ledgerx
  labels: { app.kubernetes.io/name: transaction-monitoring, ledgerx.io/domain: fraud-risk }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50140, targetPort: 50140 }]
  selector: { app.kubernetes.io/name: transaction-monitoring }
