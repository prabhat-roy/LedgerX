apiVersion: v1
kind: ConfigMap
metadata:
  name: risk-scoring
  namespace: ledgerx
  labels: { app.kubernetes.io/name: risk-scoring, ledgerx.io/domain: fraud-risk }
data:
  LOG_LEVEL: info
  PORT: "50141"
