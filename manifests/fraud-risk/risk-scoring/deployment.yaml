apiVersion: apps/v1
kind: Deployment
metadata:
  name: risk-scoring
  namespace: ledgerx
  labels:
    app.kubernetes.io/name: risk-scoring
    app.kubernetes.io/part-of: ledgerx
    ledgerx.io/domain: fraud-risk
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: risk-scoring } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: risk-scoring, ledgerx.io/domain: fraud-risk }
    spec:
      serviceAccountName: risk-scoring
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: risk-scoring
          image: harbor.ledgerx.internal/ledgerx/risk-scoring:latest
          ports: [{ containerPort: 50141, name: http }]
          envFrom: [{ configMapRef: { name: risk-scoring } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50141 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50141 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
