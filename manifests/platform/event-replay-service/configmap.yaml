apiVersion: v1
kind: ConfigMap
metadata:
  name: event-replay-service
  namespace: ledgerx
  labels: { app.kubernetes.io/name: event-replay-service, ledgerx.io/domain: platform }
data:
  LOG_LEVEL: info
  PORT: "51009"
