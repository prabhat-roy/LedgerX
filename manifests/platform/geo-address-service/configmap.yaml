apiVersion: v1
kind: ConfigMap
metadata:
  name: geo-address-service
  namespace: ledgerx
  labels: { app.kubernetes.io/name: geo-address-service, ledgerx.io/domain: platform }
data:
  LOG_LEVEL: info
  PORT: "51013"
