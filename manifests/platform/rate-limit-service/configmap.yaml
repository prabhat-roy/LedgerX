apiVersion: v1
kind: ConfigMap
metadata:
  name: rate-limit-service
  namespace: ledgerx
  labels: { app.kubernetes.io/name: rate-limit-service, ledgerx.io/domain: platform }
data:
  LOG_LEVEL: info
  PORT: "51007"
