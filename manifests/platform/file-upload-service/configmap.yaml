apiVersion: v1
kind: ConfigMap
metadata:
  name: file-upload-service
  namespace: ledgerx
  labels: { app.kubernetes.io/name: file-upload-service, ledgerx.io/domain: platform }
data:
  LOG_LEVEL: info
  PORT: "51011"
