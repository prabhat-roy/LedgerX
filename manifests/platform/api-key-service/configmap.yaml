apiVersion: v1
kind: ConfigMap
metadata:
  name: api-key-service
  namespace: ledgerx
  labels: { app.kubernetes.io/name: api-key-service, ledgerx.io/domain: platform }
data:
  LOG_LEVEL: info
  PORT: "51003"
