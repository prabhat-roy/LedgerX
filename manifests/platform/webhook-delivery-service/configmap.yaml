apiVersion: v1
kind: ConfigMap
metadata:
  name: webhook-delivery-service
  namespace: ledgerx
  labels: { app.kubernetes.io/name: webhook-delivery-service, ledgerx.io/domain: platform }
data:
  LOG_LEVEL: info
  PORT: "51004"
