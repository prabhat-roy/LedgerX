apiVersion: apps/v1
kind: Deployment
metadata:
  name: claims-service
  namespace: ledgerx
  labels:
    app.kubernetes.io/name: claims-service
    app.kubernetes.io/part-of: ledgerx
    ledgerx.io/domain: insurance
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: claims-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: claims-service, ledgerx.io/domain: insurance }
    spec:
      serviceAccountName: claims-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: claims-service
          image: harbor.ledgerx.internal/ledgerx/claims-service:latest
          ports: [{ containerPort: 50181, name: http }]
          envFrom: [{ configMapRef: { name: claims-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50181 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50181 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
