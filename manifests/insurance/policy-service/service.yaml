apiVersion: v1
kind: Service
metadata:
  name: policy-service
  namespace: ledgerx
  labels: { app.kubernetes.io/name: policy-service, ledgerx.io/domain: insurance }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50180, targetPort: 50180 }]
  selector: { app.kubernetes.io/name: policy-service }
