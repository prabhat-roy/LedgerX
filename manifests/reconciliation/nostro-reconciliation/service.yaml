apiVersion: v1
kind: Service
metadata:
  name: nostro-reconciliation
  namespace: ledgerx
  labels: { app.kubernetes.io/name: nostro-reconciliation, ledgerx.io/domain: reconciliation }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50322, targetPort: 50322 }]
  selector: { app.kubernetes.io/name: nostro-reconciliation }
