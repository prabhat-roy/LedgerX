apiVersion: v1
kind: ConfigMap
metadata:
  name: settlement-service
  namespace: ledgerx
  labels: { app.kubernetes.io/name: settlement-service, ledgerx.io/domain: reconciliation }
data:
  LOG_LEVEL: info
  PORT: "50321"
