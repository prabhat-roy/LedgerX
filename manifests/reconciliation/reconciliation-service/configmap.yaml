apiVersion: v1
kind: ConfigMap
metadata:
  name: reconciliation-service
  namespace: ledgerx
  labels: { app.kubernetes.io/name: reconciliation-service, ledgerx.io/domain: reconciliation }
data:
  LOG_LEVEL: info
  PORT: "50320"
