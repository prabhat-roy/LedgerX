apiVersion: apps/v1
kind: Deployment
metadata:
  name: reconciliation-service
  namespace: ledgerx
  labels:
    app.kubernetes.io/name: reconciliation-service
    app.kubernetes.io/part-of: ledgerx
    ledgerx.io/domain: reconciliation
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: reconciliation-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: reconciliation-service, ledgerx.io/domain: reconciliation }
    spec:
      serviceAccountName: reconciliation-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: reconciliation-service
          image: harbor.ledgerx.internal/ledgerx/reconciliation-service:latest
          ports: [{ containerPort: 50320, name: http }]
          envFrom: [{ configMapRef: { name: reconciliation-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50320 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50320 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
