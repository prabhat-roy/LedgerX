apiVersion: v1
kind: Service
metadata:
  name: market-data
  namespace: ledgerx
  labels: { app.kubernetes.io/name: market-data, ledgerx.io/domain: wealth }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50163, targetPort: 50163 }]
  selector: { app.kubernetes.io/name: market-data }
