apiVersion: v1
kind: Service
metadata:
  name: investment-service
  namespace: ledgerx
  labels: { app.kubernetes.io/name: investment-service, ledgerx.io/domain: wealth }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50161, targetPort: 50161 }]
  selector: { app.kubernetes.io/name: investment-service }
