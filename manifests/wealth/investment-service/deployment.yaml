apiVersion: apps/v1
kind: Deployment
metadata:
  name: investment-service
  namespace: ledgerx
  labels:
    app.kubernetes.io/name: investment-service
    app.kubernetes.io/part-of: ledgerx
    ledgerx.io/domain: wealth
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: investment-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: investment-service, ledgerx.io/domain: wealth }
    spec:
      serviceAccountName: investment-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: investment-service
          image: harbor.ledgerx.internal/ledgerx/investment-service:latest
          ports: [{ containerPort: 50161, name: http }]
          envFrom: [{ configMapRef: { name: investment-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50161 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50161 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
