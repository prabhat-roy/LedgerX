apiVersion: apps/v1
kind: Deployment
metadata:
  name: portfolio-service
  namespace: ledgerx
  labels:
    app.kubernetes.io/name: portfolio-service
    app.kubernetes.io/part-of: ledgerx
    ledgerx.io/domain: wealth
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: portfolio-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: portfolio-service, ledgerx.io/domain: wealth }
    spec:
      serviceAccountName: portfolio-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: portfolio-service
          image: harbor.ledgerx.internal/ledgerx/portfolio-service:latest
          ports: [{ containerPort: 50160, name: http }]
          envFrom: [{ configMapRef: { name: portfolio-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50160 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50160 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
