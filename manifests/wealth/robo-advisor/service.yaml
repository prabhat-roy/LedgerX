apiVersion: v1
kind: Service
metadata:
  name: robo-advisor
  namespace: ledgerx
  labels: { app.kubernetes.io/name: robo-advisor, ledgerx.io/domain: wealth }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50162, targetPort: 50162 }]
  selector: { app.kubernetes.io/name: robo-advisor }
