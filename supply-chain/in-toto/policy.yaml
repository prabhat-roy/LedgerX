# in-toto layout — supply-chain step attestations for LedgerX
expires: "2027-01-01T00:00:00Z"
keys: { project-key: { keyid: ledgerx-key, keytype: ed25519, scheme: ed25519 } }
steps:
  - { name: source, expected_command: ["git", "clone"], pubkeys: [ledgerx-key] }
  - { name: build, expected_command: ["docker", "build"], pubkeys: [ledgerx-key] }
  - { name: test, expected_command: ["make", "test"], pubkeys: [ledgerx-key] }
inspect:
  - { name: signed-image, run: ["cosign", "verify"], expected_materials: [], expected_products: [] }
